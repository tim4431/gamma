name: Assistant plugin package

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'plugins/gamma/**'
      - 'tools/*codex*'
      - 'tools/*plugins*'
      - 'tools/marketplace-README.md'
      - 'frontend/src/settings/codexSetup.js'
      - 'frontend/src/settings/assistantSetup.js'
      - 'frontend/tests/codexSetup.test.mjs'
      - 'frontend/tests/assistantSetup.test.mjs'
      - 'docs/dev/mcp.md'
      - 'PRIVACY.md'
      - '.github/workflows/codex-plugin.yml'

permissions:
  contents: read

jobs:
  package:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m unittest discover -s tools -p 'test_*codex*.py'
      - run: python -m unittest discover -s tools -p 'test_plugins.py'
      - run: node --test frontend/tests/codexSetup.test.mjs frontend/tests/assistantSetup.test.mjs
      - run: python tools/release_plugins.py --output dist/codex-plugin --version 0.0.0-preview
      - uses: actions/upload-artifact@v4
        with:
          name: gamma-codex-marketplace-${{ matrix.os }}
          path: |
            dist/codex-plugin/*.zip
            dist/codex-plugin/*.ps1
            dist/codex-plugin/*.sh
            dist/codex-plugin/*.tgz
            dist/codex-plugin/*SHA256SUMS.txt
          if-no-files-found: error
