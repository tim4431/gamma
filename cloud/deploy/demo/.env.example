# Copy to .env next to compose.yml. Compose reads it to fill in the image
# tag; the container never sees it (its settings are demo.env). The
# update-demo-server skill rewrites this line to the build it deploys: a
# `sha-<7 hex>` tag that docker.yml pushed (a branch build, never :latest).
GAMMA_TAG=sha-39bb6e6
