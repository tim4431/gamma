// The former import-only Gamma sheet was intentionally retired.
// GammaRootView owns login and the authenticated Gamma library for the whole app.
// NoteStore remains solely for non-destructive access to legacy local data/tests.
